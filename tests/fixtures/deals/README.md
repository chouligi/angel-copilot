# Dummy Deal Fixtures

Synthetic deal documents used for local development and regression tests.

- `alpha_ai/`: plain text and markdown docs.
- `beta_ops/`: zip-only deal input (`docs_bundle.zip`) to test automatic archive extraction.

No real or sensitive deal data is included here.
